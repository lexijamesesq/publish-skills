---
name: clean-fixture
description: Fixture artifact that should produce zero findings across every check — the baseline for regression comparison.
---

# /clean-fixture

A minimal, well-formed skill fixture. No ticket IDs, no vault paths, no
self-narration, no broken citations, no unlisted fiction entities.

## Identity

Uses only canonical sample-universe vocabulary where an example is needed:
`CacheTrack` and `Chip Chestnut`.

## Reference

Cites `SKILL.md` (itself) — a citation that always resolves.
